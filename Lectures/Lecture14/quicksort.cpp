#include <iostream>
#include <stack>
#include <queue>
#include <string>
#include <cassert>
#include <vector>
#include <algorithm>
#include <list>
#include <map>
#include <set>
#include <iterator>

using namespace std;

// quicksort N items:

if (n > 1)
{
    // pick a pivot
    // parition around the pivot
    // sort the part to the left of the pivot
    // sort the part to the right of the pivot
}


int main()
{








return 0;
}